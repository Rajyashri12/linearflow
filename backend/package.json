{
  "name": "json-server-backend",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "npx json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "0.17.3"
  }
}
